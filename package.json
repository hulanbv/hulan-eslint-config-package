{
  "name": "@hulanbv/eslint-config",
  "description": "This repository the global ESLint Configuration for all Hulan projects.",
  "author": "Hulan BV.",
  "version": "1.3.0",
  "private": false,
  "main": "distribution/eslintrc.js",
  "types": "distribution/eslintrc.d.ts",
  "scripts": {
    "test": "npm run build",
    "build": "tsc",
    "prepublishOnly": "echo \"Package should be published using GitHub actions, push a tag instead.\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "typescript": "4.3.5"
  }
}
